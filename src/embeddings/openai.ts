// Superseded by local.ts — kept as a placeholder in case a hosted embedding
// provider is reintroduced later (e.g. for higher-quality embeddings at scale).
// Currently unused: local.ts is the only EmbeddingProvider wired into the app.
export {};
